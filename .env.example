# Set securely in Vercel. Do not put values in source control or chat.
# Required for real intake: acknowledged email handoff + durable duplicate/rate controls.
LEAD_TO_EMAIL=
LEAD_FROM_EMAIL=
RESEND_API_KEY=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Random secret, at least 32 characters. Keep stable across deployments/retries.
INTAKE_SIGNING_KEY=
# Set only after owner approves production launch and domain checks.
SITE_LAUNCH=

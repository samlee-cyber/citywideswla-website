{
  "name": "citywideswla-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test scripts/*.test.mjs"
  },
  "engines": {
    "node": "24.x"
  }
}
